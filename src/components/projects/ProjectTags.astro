---
import { Tag } from "@lucide/astro";

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
const tagColors = [
  "text-[#dc8a78]",
  "text-[#dd7878]",
  "text-[#ea76cb]",
  "text-[#8839ef]",
  "text-[#d20f39]",
  "text-[#e64553]",
  "text-[#fe640b]",
];
---

<div class="flex max-h-8 flex-wrap gap-2 overflow-hidden">
  <Tag size={18} class="shrink-0" />
  {
    tags.map((tag, i) => (
      <span
        class={`shrink-0 rounded bg-(--tag) px-2 py-2 font-mono text-xs font-bold ${tagColors[i % tagColors.length]}`}
      >
        {tag}
      </span>
    ))
  }
</div>
